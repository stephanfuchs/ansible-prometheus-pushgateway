---

prometheus_pushgateway_version: 0.3.1
prometheus_pushgateway_architecture: "amd64"
prometheus_pushgateway_release_name: "pushgateway-{{ prometheus_pushgateway_version }}.linux-{{ prometheus_pushgateway_architecture }}"

prometheus_pushgateway_config_flags:
  'web.listen-address': '0.0.0.0:9091'
  'log.level': 'info'
